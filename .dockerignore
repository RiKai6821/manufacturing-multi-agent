# 构建镜像时排除这些，保持镜像精简
__pycache__/
*.pyc
*.pyo

# 本地生成的缓存和数据库（镜像内会重新构建/生成）
*.pkl
data/factory.db
data/memory.db
tools/milvus_kb.db

# 密钥（绝不进镜像；运行时通过环境变量/Secret 注入）
.env
*.key

# 虚拟环境（绝不进镜像）
venv/
.venv/

# 运行产物
last_trace.json
agent_run.log

# 开发文件
.git/
.idea/
.vscode/
*.md
